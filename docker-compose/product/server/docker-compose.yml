version: '3.8'

services:
  code-runner-server:
    image: ghcr.io/${GITHUB_REPOSITORY:-local}/coderunner-server:latest
    environment:
      - APP_MODE=server
      - APP_ENV=production
    ports:
      - "7979:7979"
      - "50011:50011"
    restart: unless-stopped
